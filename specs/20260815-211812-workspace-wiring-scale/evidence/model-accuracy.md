# Fitted resource models and held-out accuracy

Generated by `cmd/scalemodel` from the committed sweep runs in this
directory. Do not edit by hand — re-run the tool.

Every figure below is a projection from what was actually measured. The
extrapolation factor says how far past that a figure reaches, and is the
number to discount it by.

## active-heavy, 19 listener(s) per workspace

cluster-api-core / active-heavy / synthetic, 19 listeners per workspace, fitted over 8..64 workspaces (4 points): heap 3960042 B + 1541835 B/workspace (R²=1.0000), goroutines 52 + 211.0/workspace (R²=1.0000)

| Coefficient | Base | Per workspace | R² |
|---|---|---|---|
| Live heap | 3.78 MiB | 1.47 MiB | 1.00000 |
| Process footprint (`MemStats.Sys`) | 25.26 MiB | 3.54 MiB | 0.98258 |
| Goroutine stacks | 1.32 MiB | 732.97 KiB | 0.99960 |
| Goroutines | 52 | 211.00 | 1.00000 |

Footprint per workspace is **2.4×** the live-heap figure. Sizing from heap
alone would under-provision by that multiple.

### Held-out validation

Each point excluded from the fit in turn, then predicted from the rest.
The endpoints are the informative ones: they lie outside the remaining
points' range, which is the situation every published projection is in.

| Held out | Measured heap | Predicted heap | Error | Goroutine error | Outside the fit's range |
|---|---|---|---|---|---|
| 8 | 15.51 MiB | 15.57 MiB | 0.39% | 0.00% | yes |
| 16 | 27.32 MiB | 27.30 MiB | 0.08% | 0.00% | no |
| 32 | 50.86 MiB | 50.82 MiB | 0.08% | 0.00% | no |
| 64 | 97.87 MiB | 97.99 MiB | 0.13% | 0.00% | yes |

**Worst heap prediction error: 0.39%.**

### Projected sizing

| Workspaces | Live heap | Stacks | Footprint (sizing figure) | Goroutines | Extrapolation factor |
|---|---|---|---|---|---|
| 100 | 150.82 MiB | 72.89 MiB | 379.36 MiB | 21152 | 1.6× |
| 250 | 371.38 MiB | 180.26 MiB | 910.52 MiB | 52802 | 3.9× |
| 500 | 738.98 MiB | 359.21 MiB | 1.75 GiB | 105552 | 7.8× |
| 1000 | 1.44 GiB | 717.11 MiB | 3.48 GiB | 211052 | 15.6× |

Footprint basis: fitted from measured process footprint (MemStats.Sys). This is what the Go runtime obtained from the OS,
which over-estimates resident set size — the runtime returns pages lazily —
and is the right direction to err in when setting a limit.

**Not modelled.**

- CPU: the harness records no CPU time, so there is nothing to fit; the planned base + d·events·W + e·reconciles/s expression needs a sweep that measures it

## active-heavy, 1 listener(s) per workspace

cluster-api-core / active-heavy / synthetic, 1 listeners per workspace, fitted over 8..64 workspaces (4 points): heap 3555421 B + 932809 B/workspace (R²=1.0000), goroutines 52 + 13.0/workspace (R²=1.0000)

| Coefficient | Base | Per workspace | R² |
|---|---|---|---|
| Live heap | 3.39 MiB | 910.95 KiB | 1.00000 |
| Process footprint (`MemStats.Sys`) | 22.10 MiB | 1.72 MiB | 0.99903 |
| Goroutine stacks | 858.43 KiB | 41.25 KiB | 0.99961 |
| Goroutines | 52 | 13.00 | 1.00000 |

Footprint per workspace is **1.9×** the live-heap figure. Sizing from heap
alone would under-provision by that multiple.

### Held-out validation

Each point excluded from the fit in turn, then predicted from the rest.
The endpoints are the informative ones: they lie outside the remaining
points' range, which is the situation every published projection is in.

| Held out | Measured heap | Predicted heap | Error | Goroutine error | Outside the fit's range |
|---|---|---|---|---|---|
| 8 | 10.49 MiB | 10.52 MiB | 0.29% | 0.00% | yes |
| 16 | 17.63 MiB | 17.62 MiB | 0.08% | 0.00% | no |
| 32 | 31.87 MiB | 31.85 MiB | 0.05% | 0.00% | no |
| 64 | 60.32 MiB | 60.37 MiB | 0.09% | 0.00% | yes |

**Worst heap prediction error: 0.29%.**

### Projected sizing

| Workspaces | Live heap | Stacks | Footprint (sizing figure) | Goroutines | Extrapolation factor |
|---|---|---|---|---|---|
| 100 | 92.35 MiB | 4.87 MiB | 194.04 MiB | 1352 | 1.6× |
| 250 | 225.79 MiB | 10.91 MiB | 451.95 MiB | 3302 | 3.9× |
| 500 | 448.19 MiB | 20.98 MiB | 881.81 MiB | 6552 | 7.8× |
| 1000 | 892.99 MiB | 41.12 MiB | 1.70 GiB | 13052 | 15.6× |

Footprint basis: fitted from measured process footprint (MemStats.Sys). This is what the Go runtime obtained from the OS,
which over-estimates resident set size — the runtime returns pages lazily —
and is the right direction to err in when setting a limit.

**Not modelled.**

- CPU: the harness records no CPU time, so there is nothing to fit; the planned base + d·events·W + e·reconciles/s expression needs a sweep that measures it

## idle-heavy, 19 listener(s) per workspace

cluster-api-core / idle-heavy / synthetic, 19 listeners per workspace, fitted over 8..64 workspaces (4 points): heap 3630239 B + 1468782 B/workspace (R²=1.0000), goroutines 52 + 211.0/workspace (R²=1.0000)

| Coefficient | Base | Per workspace | R² |
|---|---|---|---|
| Live heap | 3.46 MiB | 1.40 MiB | 1.00000 |
| Process footprint (`MemStats.Sys`) | 19.55 MiB | 2.72 MiB | 0.99423 |
| Goroutine stacks | 864.00 KiB | 465.60 KiB | 0.99996 |
| Goroutines | 52 | 211.00 | 1.00000 |

Footprint per workspace is **1.9×** the live-heap figure. Sizing from heap
alone would under-provision by that multiple.

### Held-out validation

Each point excluded from the fit in turn, then predicted from the rest.
The endpoints are the informative ones: they lie outside the remaining
points' range, which is the situation every published projection is in.

| Held out | Measured heap | Predicted heap | Error | Goroutine error | Outside the fit's range |
|---|---|---|---|---|---|
| 8 | 14.65 MiB | 14.68 MiB | 0.19% | 0.00% | yes |
| 16 | 25.90 MiB | 25.86 MiB | 0.15% | 0.00% | no |
| 32 | 48.27 MiB | 48.29 MiB | 0.04% | 0.00% | no |
| 64 | 93.11 MiB | 93.09 MiB | 0.02% | 0.00% | yes |

**Worst heap prediction error: 0.19%.**

### Projected sizing

| Workspaces | Live heap | Stacks | Footprint (sizing figure) | Goroutines | Extrapolation factor |
|---|---|---|---|---|---|
| 100 | 143.54 MiB | 46.31 MiB | 291.13 MiB | 21152 | 1.6× |
| 250 | 353.65 MiB | 114.52 MiB | 698.50 MiB | 52802 | 3.9× |
| 500 | 703.83 MiB | 228.19 MiB | 1.35 GiB | 105552 | 7.8× |
| 1000 | 1.37 GiB | 455.53 MiB | 2.67 GiB | 211052 | 15.6× |

Footprint basis: fitted from measured process footprint (MemStats.Sys). This is what the Go runtime obtained from the OS,
which over-estimates resident set size — the runtime returns pages lazily —
and is the right direction to err in when setting a limit.

**Not modelled.**

- CPU: the harness records no CPU time, so there is nothing to fit; the planned base + d·events·W + e·reconciles/s expression needs a sweep that measures it

## idle-heavy, 1 listener(s) per workspace

cluster-api-core / idle-heavy / synthetic, 1 listeners per workspace, fitted over 8..64 workspaces (4 points): heap 3439413 B + 899528 B/workspace (R²=1.0000), goroutines 52 + 13.0/workspace (R²=1.0000)

| Coefficient | Base | Per workspace | R² |
|---|---|---|---|
| Live heap | 3.28 MiB | 878.45 KiB | 1.00000 |
| Process footprint (`MemStats.Sys`) | 20.73 MiB | 1.60 MiB | 0.99959 |
| Goroutine stacks | 788.87 KiB | 29.70 KiB | 0.99778 |
| Goroutines | 52 | 13.00 | 1.00000 |

Footprint per workspace is **1.9×** the live-heap figure. Sizing from heap
alone would under-provision by that multiple.

### Held-out validation

Each point excluded from the fit in turn, then predicted from the rest.
The endpoints are the informative ones: they lie outside the remaining
points' range, which is the situation every published projection is in.

| Held out | Measured heap | Predicted heap | Error | Goroutine error | Outside the fit's range |
|---|---|---|---|---|---|
| 8 | 10.13 MiB | 10.16 MiB | 0.26% | 0.00% | yes |
| 16 | 17.02 MiB | 17.00 MiB | 0.09% | 0.00% | no |
| 32 | 30.74 MiB | 30.73 MiB | 0.03% | 0.00% | no |
| 64 | 58.18 MiB | 58.22 MiB | 0.07% | 0.00% | yes |

**Worst heap prediction error: 0.26%.**

### Projected sizing

| Workspaces | Live heap | Stacks | Footprint (sizing figure) | Goroutines | Extrapolation factor |
|---|---|---|---|---|---|
| 100 | 89.07 MiB | 3.67 MiB | 180.26 MiB | 1352 | 1.6× |
| 250 | 217.74 MiB | 8.02 MiB | 419.56 MiB | 3302 | 3.9× |
| 500 | 432.21 MiB | 15.27 MiB | 818.39 MiB | 6552 | 7.8× |
| 1000 | 861.14 MiB | 29.78 MiB | 1.58 GiB | 13052 | 15.6× |

Footprint basis: fitted from measured process footprint (MemStats.Sys). This is what the Go runtime obtained from the OS,
which over-estimates resident set size — the runtime returns pages lazily —
and is the right direction to err in when setting a limit.

**Not modelled.**

- CPU: the harness records no CPU time, so there is nothing to fit; the planned base + d·events·W + e·reconciles/s expression needs a sweep that measures it

