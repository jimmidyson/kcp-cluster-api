/bin/

# Agent tooling generated by the spec-kit extensions (see .specify/).
# Ignored to preserve the behaviour this repository had before the inversion.
# Open question: committing these would let fresh agent containers pick them
# up without re-running setup, which is the delegation model this project is
# moving toward.
.claude/
