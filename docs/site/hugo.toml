baseURL = "https://jimmidyson.github.io/kcp-cluster-api/"
title = "kcp-cluster-api"

enableRobotsTXT = true
enableGitInfo = true

# We don't use tags/categories taxonomies for this site.
disableKinds = ["taxonomy"]

contentDir = "content/en"
defaultContentLanguage = "en"
defaultContentLanguageInSubdir = false
enableMissingTranslationPlaceholders = false

disableAliases = false

[markup]
  [markup.goldmark]
    [markup.goldmark.renderer]
      unsafe = true
    [markup.goldmark.parser.attribute]
      block = true
  [markup.highlight]
    style = "tango"
    lineNos = false
    codeFences = true
    guessSyntax = true
    noClasses = false

[module]
  [[module.imports]]
    path = "github.com/google/docsy/theme"

[params]
  copyright = "kcp-cluster-api contributors"
  privacy_policy = ""

  # Repo config used by the "Edit this page" / "Create issue" links in Docsy.
  github_repo = "https://github.com/jimmidyson/kcp-cluster-api"
  github_subdir = "kcp/docs/site"
  github_branch = "main"

  # Site search. Docsy offers Google CSE, Algolia DocSearch and this: a Lunr
  # index built at publish time and searched in the browser. It is the only
  # one of the three that needs no account, no crawler and no third-party
  # service to keep working, and the site is small enough that the index it
  # produces is a few tens of kilobytes.
  offlineSearch = true
  # The default summary is 70 characters, which truncates mid-phrase on most
  # of this site's descriptions. Long enough here to show a whole sentence.
  offlineSearchSummaryLength = 200
  offlineSearchMaxResults = 10

  prism_syntax_highlighting = false

  [params.ui]
    breadcrumb_disable = false
    navbar_logo = false
    sidebar_menu_compact = true
    sidebar_search_disable = false
    footer_about_enable = false

    [params.ui.feedback]
      enable = false

    [params.ui.readingtime]
      enable = false

[[menu.main]]
  name = "User Docs"
  weight = 10
  url = "/docs/user/"

[[menu.main]]
  name = "Design & Architecture"
  weight = 20
  url = "/docs/design/"

[[menu.main]]
  name = "GitHub"
  weight = 30
  url = "https://github.com/jimmidyson/kcp-cluster-api"
  pre = "<i class='fab fa-github'></i>"
