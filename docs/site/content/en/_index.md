---
title: kcp-cluster-api
description: Workspace-aware Cluster API for KCP
# This page only redirects, so a search hit on it would bounce the reader
# straight back out again. Keep it out of the index.
exclude_search: true
---

<!--
This page is never rendered: layouts/index.html redirects "/" to the user
docs. The file stays because Hugo needs a home page to build the site (and
to source the site's description); its content is deliberately empty.
-->
