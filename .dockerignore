# The build needs the module graph and the source, and nothing that a previous
# run of anything left behind. Two of these matter beyond tidiness: bin/ holds
# a 170MB kcp binary the image downloads for itself, and .demo/ holds a kcp
# server's state directory, credentials included.
.git
.demo
bin
docs/site/public
docs/site/resources
*.kubeconfig
